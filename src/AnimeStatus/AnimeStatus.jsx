import React from 'react'
// import Dropdown from '../DropDown/DropDown'

function AnimeStatus() {
    return (
    <div>
        <div>
            <h2>Completed Movies</h2>
            {/* <ul>
            {completedMovies.map((anime) => (
                <li key={anime.title}>{anime.title}</li>
            ))}
            </ul> */}
        </div>
        <div>
            <h2>Plan to Watch Movies</h2>
            {/* <ul>
            {planToWatchMovies.map((anime) => (
                <li key={anime.title}>{anime.title}</li>
            ))}
            </ul> */}
        </div>
      </div>
    );
}

export default AnimeStatus